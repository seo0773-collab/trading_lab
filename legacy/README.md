# Legacy Migration

Legacy code is intentionally preserved at its original paths while the
platform adapter is validated. This directory records the migration boundary;
it does not contain copied implementations.

The Wave Viewer and cycle reversion strategy remain research-only. They must
not be enabled for paper or live execution until their look-ahead profile
calculation is replaced and covered by regression tests.
